apiVersion: v1
kind: Service
metadata:
  name: {{ include "poddoctor-hub.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "poddoctor-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "poddoctor-hub.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
